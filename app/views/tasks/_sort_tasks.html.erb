<%= search_form_for @q do |f| %>
  <%= f.search_field :title_cont, placeholder: t('status_sort.by_title') %>
  <%= f.select :status_eq, ransack_status_collection, {include_blank: t('status_sort.by_status')} %>
  <%= f.submit t('buttons.search') %>
<% end %>